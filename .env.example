# 로컬에서 개발할 때만 필요합니다.
# 이 파일을 .env.local 로 복사하고 값을 채우세요.
# 배포(Vercel)에서는 프로젝트 Settings > Environment Variables 에 넣습니다.
#
# Supabase 대시보드 > Project Settings > API 에서 확인할 수 있습니다.
# publishable 키는 브라우저에 노출되는 공개 키입니다. (권한은 DB의 RLS가 막습니다)

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
